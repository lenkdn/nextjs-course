services:
  postgres:
    image: postgres:18
    container_name: nextjs-course-postgres
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nextjs_course_db
    volumes:
      - postgres:/data

volumes:
  postgres:
  
